PostEffectの流れ

通常書き込み用のRenderTargetをセット
Sprite、Mesh等の通常の描画処理

RenderTargetをResourceViewへ
ResourceViewをセット
輝度抽出用RenderTargetをセット
シェーダーを輝度抽出用に変更、頂点等はSprite用で描画

輝度抽出用RenderTargetをResourceViewへ
ぼかし用RenderTarget作成
ぼかし用に縦横適当に繰り返し半透明描画

通常ResourceViewとぼかしResourceViewをセット
合成Shaderをセット
描画

present()

SamplerDescのComparisonFuncをAlwaysにしてみる